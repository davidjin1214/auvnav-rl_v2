# 第 5 章 — 轮次入口（**本轮已闭环，下一轮入口待写**）

> ✅ **2026-08-17 当日闭环**：第 1 步（§5.9 的 $12$ 处补核）与第 2 步（选项 C 三处落地）均已完成，
> 见 [`status.md`](status.md)「待决与登记项」离散度那一行的末段。**本文件描述的任务不要再执行一遍**；
> 下一轮的 scope 由用户定，届时按体例整体重写本文件。以下内容保留作该轮的执行记录。
>
> 与执行前的方案相比有两处按实核收窄，已写入 `setup.tex` rev.10 / `rebrac.tex` rev.8 头注：
> ① §5.9 补核结果是 **ddof=1**（不是「大概率 ddof=0」的先验）；
> ② 声明里指代 §5.5 两表用**描述＋节号**而非 `\ref{tab:...}`，否则 floats/order 双判 FAIL。
> 新登记项一条：`ch5_lexicon_baseline.json` 长期未刷新，lexicon 门槛已失效（见 status.md）。

---

## 【已闭环】原文如下（当轮：离散度口径按选项 C 落地）

> 体例（2026-07-07 起）：本文件**每轮重写**，只含当轮任务与相关约束。章状态账本 = [`status.md`](status.md)（唯一真相源）；逐轮历史 = git log 与各节头注；prompt 方法论 = [`prompt_playbook.md`](prompt_playbook.md)。
> 上一轮（数据完整性整改批的独立对抗复审）已闭环——不通过 → 按批修复 → 复编译通过，0 CRIT / 1 HIGH / 4 MED / 2 LOW 全部落地，存档 [`notes/data_integrity_batch_review_findings.md`](notes/data_integrity_batch_review_findings.md)。

---

## 走到哪儿了

第 5 章十节成文、送审就绪判定成立（编译 $65$ 页）。章外三条数据完整性问题（①③⑤）已裁决走 ①-c 并整改落地、复审闭环。

**本轮的由来**：2026-08-17 对三份 GT 报告做数字级回溯时查出全章 ± 存在两种离散度口径，登记为待决项；同日出呈报、用户裁决**走选项 C**。

- 呈报（口径全表 ＋ 四选项代价 ＋ 建议）：[`notes/dispersion_convention_decision_memo.md`](notes/dispersion_convention_decision_memo.md)
- 复算工具：[`tools/ch5_dispersion_audit.py`](tools/ch5_dispersion_audit.py)（数字不写死，现场解析 `sections/*.tex`）
- 裁决与落地范围：[`status.md`](status.md)「待决与登记项」离散度那一行

---

## 本轮任务：两步，第 1 步是第 2 步的前置

### 第 1 步（前置）：补核 §5.9 的 $12$ 处 ± 口径

**不核就写不出完整的口径声明**——C 的声明要逐节写明归属，§5.9 现在是空白。

- 对象：`algo_compare.tex:263–265` 质量阶梯表的九个非零值 ＋ 正文 L270 的两处引用 ＋ L272 的 $0.987\pm0.006$。
  （同文件的 $0.742\pm0.198$ 承 §5.7 β 网格已核为 ddof=0；L262 三个 $0.000\pm0.000$ 零方差、口径无关。）
- 已知：FQL P2 结果报告里**检索不到**这批带 ± 的刊值，源头未定位，无就地口径注。
- 办法：照 §5.5 A0 那次的路子——找到该批 run 的汇总目录，用逐 run 记录（`ablation_runs.csv` 同构）
  重算 `pstdev` / `stdev` 与刊出的 `*_std` 对拍。A0 那次的实证路径可作模板：
  `experiments/protocol_screen_v2/A0_single_u10_cross_tgt15/efficiency_v2/summary/`
  （纯读文件，分钟级。原文此处注「`experiments/` 是指向 `D:\rl_v2_data` 的链接」——
  2026-08-18 迁移后链接已撤，它是工作副本内的真目录。）
- 若源目录本机没有：**如实登记「源未定位、口径未核」并在声明里写明**，不要推测口径。

### 第 2 步：落地选项 C

**C 的定义就是不动任何刊值**——这是本轮的第一条红线，任何「顺手统一一下」都出界。

| 处 | 改什么 |
|---|---|
| `setup.tex` §5.3.6（协议之家） | 增一段口径声明：本章 ± 一律为跨随机种子标准差；**逐节写明口径归属**（§5.5 A0 表、§5.6、§5.7 除两格外为总体口径；§5.5 临界表、§5.8、§5.7 那两格为样本口径；§5.9 按第 1 步结果填）；两口径相差 $\sqrt{n/(n-1)}$，同 $n$ 表内排序与倍数不变；**跨节只作均值比较、不作 ± 的跨节比较** |
| `rebrac.tex` §5.7.m | 现有披露句（$0.026$ / $0.255$ 为样本口径、总体约 $0.023$ / $0.180$）**保留不动**，加一句指针回 §5.3.6 |
| `tab:ch5_rebrac_perseed`、`tab:ch5_rebrac_screen` 两处 caption | 点明「按总体口径」——这两张表就地印了逐种子值，是全章唯一的硬敞口（读者复算得 ddof=1，与刊值对不上，共 $17$ 行） |

**§5.7.4 的「放大约 $12$ 倍」保留不动**：该句本就自注了「两者口径不一，只作离散度量级的读数」，C 下无需改。

---

## 硬约束

1. **零刊值改动**。落地后用 `tools/ch5_dispersion_audit.py` 与正文数字多重集比对自证零漂移。
2. **数字纪律**：不凭记忆、不以成稿自述或头注为准，一律回查 GT 报告或逐 run 记录。呈报里的数也要回查——它是本轮的输入，不是权威源。
3. **反 churn**：某段已合格就明说「保留不动」。§5.7.m 与 §5.7.4 两处已经诚实，别重写。
4. **工程交付**：locked `.tex` 用 Edit 不用 Write；`latexmk -pdf -xelatex` 须 $0$ undefined ref / $0$ undefined citation，成功后 `latexmk -c`。
5. 易复发错误：**§5.3.6 正文从未写过「独立测试集」**，别写回去。

---

## 不属于本轮（勿顺手开）

- **污染面全域封闭**：需 Drive 侧实跑一次 `python -m scripts.audit_seed_overlap`（对帐层与账本已就位，`8d03fcc`），销号条件见 [`../../docs/data_integrity_open_items.md`](../../docs/data_integrity_open_items.md) 文首订正 (5)。本机做不了。
- ①-c 的补充终检**已闭环**（`6618161`），δ 差中差 $+0.80$ pp 已进正文，**不要重跑**。
- 遗留但不阻塞：补充终检只覆盖 ReBRAC-Q 两格，TD3+BC／纯 BC 未在该组实例上重评。

---

## 相关提交

`6618161`（①-c 补评读数 ＋ 复算工具）· `b3eaaeb`（①③⑤ 整改批闭环）· `3d1f83b`（五个补取数据集核对）· `1da23af`（离散度口径登记待决）· `8d03fcc`（枚举对帐层 ＋ 账本 ＋ 故意喂假枚举的测试）· `af6ee64`（离散度裁决呈报 ＋ 审计工具 ＋ 登记行三处订正）
