# 数据完整性整改批 —— 独立对抗复审 findings（2026-08-16）

> 审查对象：提交 `b3eaaeb`「数据完整性整改批闭环——①③⑤ 合批落地，走 ①-c 披露路线」触及的 4 个 `.tex`、9 处。**不是整章重审。**
> 会话性质：冷启动独立复审（`prompt_playbook.md` §2 第 2 步 / §4.2 骨架）。数字一律由两个复算工具实时重出，不采信头注 rev 块与 `status.md` 自述。
> 判定：**不通过 → 按批修复 → 复编译通过**。**0 CRIT / 1 HIGH / 4 MED / 2 LOW**，全部已落地。

---

## 1. 复算基线（本轮实际执行）

| 工具 | 执行 | 结果 |
|---|---|---|
| [`tools/ch5_clean_probe_readout.py`](../tools/ch5_clean_probe_readout.py) | ✅ 本机 `mytorch1` | 内置 `EXPECTED` 断言未触发；两格 clean 均值 $0.8620$ / $0.8700$ |
| [`tools/ch5_holdout_split_audit.py`](../tools/ch5_holdout_split_audit.py) | ✅ 本机 `mytorch1` | 18 单元，其中 `ReBRAC world priv` 仅 3/5 种子在本机 |

另作的口径实核（非工具覆盖项）：

- `scripts/collect_offline_data.py:315` `episode_seed = base_seed + ep`；两数据集 `metadata.json` 均 `seed=0`、$1000$/$2000$ 回合 → §5.3.5 的连续编号与区间覆盖断言成立；
- `benchmarks/single_u10_cross_tgt15_ep100.json` 种子 $1250$–$1349$、`clean_probe/..._s3000.json` 种子 $3000$–$3099$、各 $100$ 条；
- **未追踪文件 `benchmarks/clean_probe/_repro_check_s1250.json` 是「其余生成参数与主评估集一致」的复现证据**——以种子 $1250$ 重生成，`reset_options` 与主 manifest 逐条一致。建议入库（本轮未擅自 `git add`）。
- `noisy_support_screen` 的 `validation/`、`test_selected/`、`test_bc_selected/`、`baselines/` 四类 json，其 `eval_episode_results` 全为 seeds $1250$–$1289$、各 $40$ 回合 → §5.6 含噪筛查确为 val≡test，**两个数据规模皆然**；
- `analysis_compare/noisy_support_comparison.json` 复现 §5.6.2 四个端点：$1000$ 格 BC $0.675$ / TD3+BC $0.7625$ / 含噪双侧 $0.5125$；$2000$ 格 BC $0.60$ → 含噪 $0.7375$。

---

## 2. HIGH-1 ｜ §5.6 的闭合比例四处未随批整改，收束层无留出限定

**事实。** 本批把 §5.7.2 的「过半」改「约半」，判据是 $53.0\% \to 48.9\%$（位移 $-4.1$ pp）在留出集上不再过半。**同一次复核下，TD3+BC 特权协议的同一比例由 $48.5\% \to 35.6\%$，位移 $-12.9$ pp，为前者三倍**——复算：$(0.8867-0.8333)/(0.9833-0.8333)=35.6\%$。而它在全章有四处等价强度表述，无一带口径：`td3bc.tex` §5.6.4 正文与表 caption、`discussion.tex` §5.10.2 正文与表 5.21 第 2 行。§5.6.m 原只登记了两处回落，**未登记这个比例**。

**为何是 HIGH。** 同时命中两条指名维度：第 3 维（收束层不得强于源头——§5.10.2 无限定）与第 4 维（全章是否还有等价强度引用——有，且是位移最大的那个）。整改面按「本批改了哪些段」划，而不是按「这次复核动了哪些读数」划，于是位移最大的读数整批漏网。

**处置（走 ①-c 同构的披露路线，非弱化）。** $48.5\%$ 点估计与「约一半 / 之半」措辞全部不动——主评估集口径下仍为真；只补留出口径：

| 位置 | 改动 |
|---|---|
| `td3bc.tex` §5.6.4 正文 | 「约一半」后补：系主评估集口径，留出回合上降至 $35.6\%$，方向不变而不再合于一半 |
| `td3bc.tex` §5.6.m | 新增一句登记 $48.5\% \to 35.6\%$，与既有 $-7.6/-5.4 \to -9.0/-6.0$ 同段 |
| `discussion.tex` §5.10.2 正文 | 同上，作指针式转述（不新立说法、不解除限定） |
| `discussion.tex` 表 5.21 第 2 行 | 「证据强度限定」列补：闭合比例系主评估集口径，留出回合上为 $35.6\%$ |
| `td3bc.tex` 表 caption | **保留不动**（反 churn）：其末已写「其统计显著性尚未确立（见正文）」，正文口径句紧邻 |

→ 本次仍**无第二处论证强度改动**。

---

## 3. MED

### MED-1 ｜ `0.025` 应为 `0.024`（`rebrac.tex` §5.7.1、§5.7.m 各一处）

clean cross-2000 逐种子 $[0.89,0.87,0.86,0.83,0.90]$ → 总体口径 $\sigma = 0.024495$，三位小数为 $\mathbf{0.024}$；样本口径为 $0.027$。$0.025$ **两个口径都不是**，系工具四位显示 `0.0245` 再取整的双重取整。同段 $0.019$（$0.019391$）无误。均值与全部差值口径不受影响。✅ 已改。

### MED-2 ｜ §5.3.6 / §5.10.4 的全称断言超出实际复核面

原文：「把**每个**终检读数……重算**各处**组间比较……不改变**本章任何**结论的方向」。源头 §5.7.m 用的却是**闭合枚举**（「本章相关比较……：」+ 四项 + 一例外）。实核：

- §5.5 / §5.8 / §5.9 各自明写**直接评估训练终点检查点、不作验证集选点**（`online.tex:371`、`boundary.tex:256`、`algo_compare.tex:295`）→ 本不受该嵌套影响；
- 但 §5.8 亚临界一侧的 $30$ 回合评估集**整体落在前 $40$ 条之内**，结构上无留出回合可拆——「每个终检读数」经不起追问；
- `ReBRAC world priv` 单元本机只有 seeds 42/43/44（`rebrac.tex` rev.3 头注已记 45/46 未同步），该单元的 $0.934$、$+12$ pp 救援、$57.6\%$ 均不在复核枚举内。

✅ 已改：§5.3.6 收窄为枚举口径并写明三节的豁免理由与 §5.8 的结构性不可拆；§5.10.4 改「在 §5.7.m 所枚举的各处比较上」。

> 这正是硬约束里「枚举支撑的全称断言不可信」的同型错误——只是这次枚举的是**读数**而非文件。

### MED-3 ｜ §5.7.m 把采集器的 $2$ pp 当作可减项

原文「即抽样一项约占 $2$ 个百分点……**余量则归于后者，其量级与训练实例重合本身相当**」。两处问题：

1. `ch5_clean_probe_readout.py` 结尾注释明确反对：「bounding the draw component as small, **not as a subtraction**」。理由工具自带实证——同批评估回合上，选点集与留出回合之间的位移在采集器（$-0.083$）与各策略（一律为正）之间**符号相反**，难度位移不跨能力水平传递。
2. 「量级相当」不成立：余量 $2.0/2.8$ pp 对训练实例重合的 $0.80$ pp，差三倍以上。

✅ 已改：只把抽样成分界定为小量上界、不作可减项，给出符号相反这一理由，余量归于选点适配但不再拆份额。

### MED-4 ｜ §5.6.2 的 val≡test 指针不承载

原文以 §5.7.m 为「该筛查协议验证集与测试集为同一批实例」的出处，但 §5.7.m 那句写的是「**本节的**筛查协议（表 5.11）」＝ §5.7 的 $\beta$ 网格，不覆盖 §5.6 含噪筛查。**事实本身经结果文件实核为真**（见 §1），故属指针问题而非事实错。✅ 已改：指针改指 §5.6.m，并在 §5.6.m 就地登记。

---

## 4. LOW

- **LOW-1** §5.7.2「该比例是本节唯一对选点集嵌套**敏感**的读数」严格说不唯一：同节 $57.6\%$（特权协议 ReBRAC-Q）为同一比例第三个取值且未进复核枚举，各格绝对水平在留出口径下亦位移 $2$–$3.5$ pp。✅ 改「唯一因选点集嵌套而须**改变限定表述**的读数」。
- **LOW-2** §5.6.2 只对两千回合一侧点明 val≡test，但该限制对一千回合一侧同样成立（已实核），故作判别性反证的 $0.68/0.76 \to 0.51$ 也不是留出读数。✅ 补半句：反证的判别力在于两个规模上方向相反，而非任一端点的绝对水平。

---

## 5. 逐句核过、判定合格、保留不动

- **第 2 维 口径不越权：完全守住。** §5.7.1 与 §5.7.m 两处均主动声明 TD3+BC 未在补充实例上重跑、不重述其基线差距；§5.7.1 明写「承载判断的是两格之差而非各自绝对水平」；全批无一处跨评估集比较绝对值。
- **第 1 维 数字忠实（除 MED-1 外全部重出）**：$0.862\pm0.019$、翻转 $+1.6\to+0.8$、差中差 $+0.80$ pp / $t=0.512$ / CI $[-3.54,+5.14]$、$t=0.43$ 与 $1.06$、下移 $4.0/4.8$ pp 且各自五种子无一反向、采集器 $0.950\to0.930$、嵌套复核 $28.0/40.0$、$+3.0$（五种子无一为负，含一颗恰为 $0$——「无一为负」措辞正确，非「全为正」）、$-9.0/-6.0$、$48.9/35.6$。
- **第 5 维 术语**：「留出回合」§5.3.6 先定义后用；「补充终检」§5.7.m 引入、§5.10.4 才用，无逆序；§5.7.1 描述该程序时未抢用术语并给出指回 §5.7.m 的指针。
- **第 6 维 红线**：$48.9\%/35.6\%$ 与绝对水平下移均在正文同台呈现，未降级为附带说明。
- §5.7.m 的嵌套复核段、筛查表 caption、§5.3.5 的种子区间段：逐句核对通过，**保留不动**。

---

## 6. 验收

- 编译 **$65$ 页**、$0$ undefined ref / $0$ undefined citation、$2$ 处 Overfull（$3.1251$ / $4.31058$ pt，与整改前逐条相同），`latexmk -c` 已清。
- **既有数字零漂移**：四文件正文（排除注释行）数字多重集与 `HEAD` 逐一比对，唯一删除项为 `rebrac.tex` 的 `0.025`×2（即 MED-1 订正），其余新增项全部为 $35.6$×3、$48.5$×1 与 `\ref` 标签内的数字，无一既有数字被删改。
- 头注 rev 块：`setup` rev.8 / `rebrac` rev.6 / `td3bc` rev.8 / `discussion` rev.4，逐条记录病因与处置。

## 7. 遗留与 Drive 取回清单（**三项已全部闭合**，结果见 §8）

第 3 项闭环于 `9453f7e`；第 1、2 项文件已于 2026-08-16 取回，实算结果见 §8。以下取回清单保留为记录。

### 遗留 1 ｜ 含噪 $2000$ 集与评估集逐条相同，本机无法复核

`audit_seed_overlap` 的 identity pass（`run_identity_pass`）**只读 `offline_data/<集名>/metadata.json`** ＋ benchmark manifest ＋ 尾流场；**不读 `transitions.npz`**（源码注释：转移数组不存 `flow_time`/`start_xy`，故重放 reset 是唯一比对途径）。尾流场 `wake_data/wake_v8_U1p00_Re150_D12p00_dx0p60_Ti5pct_1200f_roi.npy` 本机已有。

取回（各约 $1.5$ KB，**放回同名目录即可**，range pass 按 `offline_data/*/metadata.json` 枚举）：

```
offline_data/crosscomp_s0_h4_efficiency_v2_re150_u10cross_fixdone_noise0p05clip0p15_ep2000/metadata.json
offline_data/crosscomp_s0_h4_efficiency_v2_re150_u10cross_fixdone_noise0p05clip0p15_ep1000/metadata.json
```

第二个是为 §5.6.2 判别性反证那一侧的「与评估集不相交」补同等强度的证据（当前该断言仅由 $\le1000$ 回合的通则推得，未逐集实核）。identity pass 用到的键在本机同类文件中齐全：`seed` / `num_episodes` / `flow_path` / `history_length` / `probe_layout` / `task_geometry` / `target_speed`。

复算命令：

```bash
python -m scripts.audit_seed_overlap --verify crosscomp_s0_h4_efficiency_v2_re150_u10cross_fixdone_noise0p05clip0p15_ep2000 single_u10_cross_tgt15_ep100
```

> **顺带**：若把 Drive 上**全部** `offline_data/*/metadata.json` 一并取回（每个约 $1.5$ KB），range pass 即可在本机对全集跑一遍——这正是「污染面枚举因 Drive FUSE 列举残缺未封闭」那条所缺的东西。本机 `os.listdir` 逐名 stat 可靠，不复现 FUSE 的枚举残缺。

### 遗留 2 ｜ `ReBRAC world priv` 单元 seeds 45/46 未同步

该单元的 $0.934 \pm 0.026$、难例种子 $+12$ pp 救援、离散度 $0.077 \to 0.026$、闭合比例 $57.6\%$ 均不在留出复核枚举内（本机只有 seeds 42/43/44）。取回两个文件（各约 $70$ KB，含 `eval_episode_results` 逐回合记录）：

```
results/offline/rebrac/worldcomp_teacher_gap/privileged_critic/worldcomp_s0_h4_efficiency_v2_re150_u10cross_fixdone_ep1000/actorb_4p0__criticb_2p0/test/seed_45.json
results/offline/rebrac/worldcomp_teacher_gap/privileged_critic/worldcomp_s0_h4_efficiency_v2_re150_u10cross_fixdone_ep1000/actorb_4p0__criticb_2p0/test/seed_46.json
```

对照面（`world dep` 五种子、`TD3+BC world dep/priv` 五种子、`collector worldcomp`）本机齐全，取回后直接 `python paper/thesis_ch5/tools/ch5_holdout_split_audit.py` 即出 $5$ 种子读数，该单元行的 `[3 seeds local]` 标记随之消失。

> ⚠ 取回后须复核一处：本机三种子上 `world priv` 的 hold60 均值 $0.9000$ 与 `world dep` 五种子的 $0.9067$ **不可直接比**（种子数不同）；按共同种子 42/43/44 配对则 dep $0.8722$、priv $0.9000$，即 $+2.8$ pp，与已刊的 $+0.6$ pp **同向且更大**。五种子齐全后应重算，若方向仍保持，§5.7.2「特权观测未再表现出可检测的均值抬升」不受影响，可把该单元并入 §5.7.m 的复核枚举。

---

## 8. 遗留闭合实算结果（2026-08-16，文件取回后）

### 8.1 遗留 1 ｜含噪集 —— 通过

`audit_seed_overlap --verify`，本机独立复现 Drive 侧结论：

| 数据集 | 种子区间 | 与主评估集 |
|---|---|---|
| `..._noise0p05clip0p15_ep2000` | $0$–$1999$ | **$100/100$ 逐条相同** |
| `..._noise0p05clip0p15_ep1000` | $0$–$999$ | $0/100$ 不相交 |

§5.3.5 与 §5.6.2 的两条断言均成立。一千回合一侧此前只由「$\le1000$ 回合」的通则推得，现已逐集实核——§5.6.2 判别性反证所依的「该规模与评估集不相交」拿到了与两千回合侧同等强度的证据。

### 8.2 遗留 2 ｜`world priv` 补齐五种子 —— 出第二处例外，正文已并入枚举

`full100` $= 0.9340$（总体 $0.023$／样本 $0.026$），与正文 $0.934 \pm 0.026$ 及 §5.7.m 的两口径说明逐字吻合。`hold60` 上：

| 读数 | full100 | hold60 | 判定 |
|---|---|---|---|
| 难例种子救援 | $0.780 \to 0.900$（$+12$ pp） | $0.750 \to 0.883$（$+13.3$ pp） | 同向增强 |
| 其余四种子无一上升 | 成立 | 成立 | 保持 |
| 种子级离散度收窄 | $0.077 \to 0.026$ | $0.085 \to 0.025$ | 同向增强 |
| 两协议均值差 | $+0.6$ pp | $\mathbf{-0.3}$ **pp** | **变号** |
| 特权侧闭合比例 | $57.6\%$ | $46.7\%$ | 下降 |

**处置**：均值差变号使 §5.7.m 的「唯一的例外」可证伪 → 改「两处例外」，第二处如实登记（`rebrac.tex` rev.7），§5.3.6／§5.10.4 同批对齐（`setup.tex` rev.9、`discussion.tex` rev.5）。

**未构成论证强度改动**：翻转的是本章明确声明不作效应解读的点估计，§5.7.2 的判读是「未检测到可检测的均值抬升」，变号只使该 null 更稳；支撑该节机制读法的救援与离散度两项在留出回合上均**同向增强**。§5.7.2「唯一因选点集嵌套而**须改变限定表述**的读数」（rev.6 LOW-1 所改）经复核**仍成立**——均值差变号未改变任何限定表述——故该句保留不动。

复算工具同批补入 §5.7.2 的 ReBRAC dep/priv 块（此前只列该单元、不作分析），新增读数全部可重出。

### 8.3 新发现 ｜`audit_seed_overlap` 漏扫嵌套数据集（已修 `0aa42ac`）

`_load_datasets()` 用单层 `glob("*/metadata.json")`，而 `_load_manifests()` 用 `rglob` —— **两侧不对称**。`offline_data/fql_succession/` 与 `audit_dryrun_2026-05-19/` 把数据集嵌在第二层，**其下 12 个集从未进过污染面枚举**。

修后本机枚举 $10 \to 24$ 个集，新曝光的 12 个**全部 clean**，OVERLAP 仍只有两格且均为 $100/100$——章内结论不受影响，但此前的「已枚举」是虚的。

> ⚠ **订正（同日，本条初稿判错）**：初稿称「这是『污染面枚举未封闭』的真正根因，与 Drive FUSE 无关」。**该断言不成立**，回查事实账本文首 ⚠⚠ 块后推翻：Drive 侧 range pass 报 `datasets: 24`，而漏掉的含噪 $2000$ 集在 Drive 上是**顶层目录**（`--verify` 用裸名即解析成功）——单层 `glob` 本应扫到它。本修复解释不了 Drive 那次。
>
> 正确的图景是**两个彼此独立的缺陷**：
> 1. **Drive 侧**：`glob` 漏掉一个确实存在的顶层目录，成因未证实，FUSE 列举残缺的假设**依然成立、依然未闭**；
> 2. **本机侧**（本条发现）：单层 `glob` 结构上看不见嵌套集，$12$ 个集从未进过枚举。已修。
>
> 修复 2 使**本机**枚举可信，既不证实也不推翻 1。

> 与「枚举支撑的全称断言不可信」同型的第三个实例：前两个是 Drive 侧的 `glob`、复核读数的 universal 化，这次是审计工具自身的扫描深度。**审计工具的覆盖面本身也要被审计**——而这条订正说明，连「找到了根因」本身也要被追问一次：找到*一个*成立的成因，不等于它就是*那个*成因。

### 8.4 五个补取数据集的核对结果 —— **全部通过，正文无需改动**

五个 `metadata.json` 已于 2026-08-16 取回。本机 $29$ 个数据集全部参与 range pass，**OVERLAP 仍只有两格**（历史效率导向 $2000$ 回合确定性集与其含噪变体，各 $100/100$），其余 $27$ 个全部 clean。

**封闭到什么程度，须说准**（承 §8.3 的订正）：

- ✅ **第 5 章的污染面已封闭**。以 `results/offline/**` 的目录名反查章内每个训练单元所依的数据集，逐名与本机 `offline_data/` 对拍，**无一缺失**（剩余未匹配项经逐个查证均为结果目录结构名，非数据集名）。章内结论所依的数据集，现在全部在本机、全部已审。
- ❌ **仓库全域的枚举仍未封闭**。Drive 顶层单层 `glob` 就报 $24$ 个集（且至少漏了 $1$ 个），本机含嵌套共 $29$ 个，两个集合并不相同——Drive-only 且章外的数据集仍未扫到。此项与本章送审无关，但事实账本文首的 ⚠⚠ 块**不能销号**。**（2026-08-21 追注：已销号。Drive 侧实跑 $35$ 集 × $24$ manifest，三个零成立；漏项成因实测为符号链接而非 FUSE，`06e6d1f` 修。）**

**① 五百回合集** `crosscomp_..._u10cross_fixdone`：seed $0$、$500$ 回合 → 种子 $0$–$499$，与主评估集 $1250$–$1349$ 不相交。§5.6.m「五百与一千回合两个规模与评估集不相交」**由通则推得改为逐集实核**。

顺带把 §5.6 数据规模轴的采集成功率与源头 `metadata.json` 对了一遍，表 5.10 三行**逐格精确**：

| 规模 | metadata `success_rate` | 表 5.10 | §5.6.2 正文 |
|---|---|---|---|
| $500$ | $0.864$ | $0.864$ | 约 $0.86$ |
| $1000$ | $0.870$ | $0.870$ | 约 $0.87$ |
| $2000$ | $0.8855$ | $0.886$ | 约 $0.89$ |

**② TD3+BC 侧 worldcomp 集** `worldcomp_..._fixdone` vs ReBRAC 侧 `worldcomp_..._fixdone_ep1000`：

- `metadata.json` **$30$ 个键零差异**（含 `seed`／`num_episodes`／`num_transitions`／`success_rate`／`mean_return`）；
- `transitions.npz` 的 hash 与大小**不同**，遂逐数组比对 → **$12$ 个共有数组全部逐元素完全相同**；差异只在 `_ep1000` 多出一个 `next_actions`（$104360 \times 2$，ReBRAC 所需），恰好解释那 $\sim370$ KB。

→ §5.7.2「同一数据上的对照」**字面成立**：两侧训练于同一批转移，`_ep1000` 只是多带一个派生字段的副本。此前只是目录名不同，非数据不同。

> 方法上值得记一笔：metadata 全等**不足以**断定数据同一（这里 hash 就不同），hash 不同也**不蕴含**数据不同。两级都查过才能下结论。

**③ 三个章外集**（`c1_*` 上游消融、`s1` 传感、`phase0` 无 `_fixdone` 遗留）全部 clean。其中 `crosscomp_..._u10cross`（无 `_fixdone`）与五百回合集的 $19$ 个键相同、$11$ 个键差异全为「旧 schema 缺字段」（`None` vs 有值），`num_transitions`／`success_rate`／`mean_return` 一致——同一次采集的旧版元数据副本，非独立数据集。两个上游集 `num_transitions` 同为 $268329$ 亦属正常：采集器是解析控制器，其动作不依赖奖励设定与探针布局，同种子必得同轨迹。

**结论**：五项核对全部通过，**未触发任何正文改动**（反 churn）。事实账本「污染面枚举未封闭」一条**按章内范围可销、按仓库全域不可销**，销号措辞须按上文两条分别写。
