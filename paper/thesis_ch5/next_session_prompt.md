# 第 5 章续写 — 本轮入口 Prompt（全章复审整改 · 第 1 批：CRITICAL 事实订正）

> 体例（2026-07-07 起）：本文件**每轮重写**，只含当轮任务与相关约束。章状态账本 = `status.md`（唯一真相源）；逐轮历史 = git log 与各节头注；prompt 方法论 = `prompt_playbook.md`。
> **流程状态**：全章整体复审轮已闭环（2026-07-21），findings 存档 [`chapter_full_review_findings.md`](chapter_full_review_findings.md)，整体判定 = **有条件通过**，阻塞性必修 5 条（C1/C2/H1/H2/H3），全部为文本层、无一需补实验。用户拍板**分批整改**，本轮执行第 1 批。

---

## 你是谁

你是这份博士论文第 5 章的合作者，熟悉本仓库的写作纪律：已定稿 `.tex` 视为 locked、数字一律溯源 ground truth、改动分轻/重流程。本轮你执行的是**外审复审后的第一批整改**，改动性质是**纯事实订正，零论证改写**——因此走**轻流程**：直接落地 → 自查 → 编译 → commit，不需要先出方案报批。

## 本轮任务：第 1 批 · CRITICAL 事实订正（＋三条顺手 MED）

改动对象与逐条改法**已在 findings 中写死**，本轮只需精确执行 + 独立验证，不需要重新论证要不要改。

### 必做（阻塞项）

**C1 · `sections/discussion.tex` 三处「两种子」→「三种子」**

§5.8 自 2026-07-12 补种子起为三种子（`{42, 0, 43}`），`discussion.tex` 漏改。三处：

1. §5.10.3 正文：「引用这一临界结论须连同其**两种子**限定」
2. 表 5.21 `tab:ch5_discussion_priv_states` **第 4 行**（离线临界 ReBRAC-Q）证据强度列：「**两随机种子**；零成功为零方差强信号」
3. §5.10.4：「全章种子规模沿五、三、二的阶梯递减，**临界边界**与个别消融是**两种子**的存在性证据」——此句需改到不再把临界边界归入「二」档（「二」档应只剩层归一化消融与 §5.9 矩阵单元）

> ⚠ **表 5.21 第 1 行「两随机种子配对复现」（在线临界 · 标准 SAC 特权消融）是正确的，不要连坐改。** 该单元确为 n=2（`arrival_v2_experiment_report.md` §7.7 的 2-seed paired hardening）。

**C2 · 三处「独立采样 / 样本不重合」改为「嵌套子集」**

复审已实核：`benchmarks/single_u10_cross_tgt15.json`（30 回合）除 `episode_id` 外与 `single_u10_cross_tgt15_ep100.json` **前 30 条逐条等同**，是严格子集。三处：

1. `sections/boundary.tex` §5.8.1：「每种子 $100$ 回合对 $30$ 回合且**样本不重合**」
2. `sections/boundary.tex` §5.8.m：「取自同一任务分布、但为**独立采样**且规模不同（30 对 100），**样本并不重合**」
3. `sections/algo_compare.tex` §5.9.m：「与主评估集同任务分布、**独立采样**的三十回合固定评估集」

改法方向：改为「30 回合集为 100 回合主评估集的固定前 30 条子集，两者任务实例嵌套而非独立」。
**顺势收益**：§5.8.1 现以「样本不重合」为「只宜作量级参考」的论据之一，嵌套关系意味着**可配对性更强**——请顺带把该句的限定理由调整到仍然成立的那几条上（跨奖励设定、跨种子数、跨评估规模），不要让论证悬空。

### 顺手清理（同属零论证事实订正，一并做）

- **M8** · `sections/algo_compare.tex` §5.9.3 末：删「追加种子会收紧这些无差异区间，**而不会翻转其方向**」的后半句（$n{=}2$ 下不可支撑的预测）。
- **M9** · `sections/rebrac.tex` §5.7.m：std 口径声明「其余单元均为总体口径」对层归一化消融不成立（$0.740\pm0.255$ 为**样本**口径，总体为 $0.180$）。订正为「特权协议 ReBRAC-Q 与层归一化消融两个单元为样本口径，其余为总体口径」；同时 §5.7.4 的「离散度放大约 12 倍」系样本 std ÷ 总体 std 的混合比较，请就地加半句口径说明（**不要改数字**——GT 自身即此算法）。
- **M16** · `sections/online.tex` §5.5.4：「无论历史长度与初始化如何均一致越界」的实证基底只含 $k\in\{8,12\}$（GT §7.9.7 的 5 个 run = k12×{42,0,7} + k8×{7,42}，不含 k=4）。收窄表述。

## 硬护栏

1. **零论证改动、零数字改动**。本批全部是「同一事实的错误陈述 → 正确陈述」。若你在执行中认为某处必须动论证或动数字，**停下呈报**，不要顺手改——那属于第 2–4 批的重流程范围。
2. **不得从记忆写数字**。任何需要核对的刊值实时回查：`docs/rebrac_experiment_report.md`（§5.7）、`docs/rebrac_broad_validation_v2_report.md`（§5.8）、`docs/arrival_v2_experiment_report.md` §7.10（§5.5 临界传感）、`docs/td3bc_phase0c_experiment_report.md`（§5.6）、`docs/fql_succession_p2_results.md`＋`docs/arrival_v2_sac_collector_design.md`（§5.9）、`docs/online_rl_line_summary.md` §1.1（A0）。**不以 `.tex` 头注自述为准。**
3. **locked `.tex` 只 Edit 微修，不 Write 覆盖**；`paper/sections/`（paper 1 素材）只读勿改。
4. **不碰已锁决策**（零跨章、生物只作引子 + §5.10 一次回扣、NO APPENDIX、术语「机体」、贡献用论证性散文、不写自我贬抑句）。findings §5 的三条正面挑战（CH1/CH2/CH3）**本轮不执行**，等用户裁断。
5. 语体守 spec §0.5.9／§0.5.10（报告体 marker 零出现、弯引号、序数枚举溶解）。

## 完成标准

- [ ] 六处改动（C1×3、C2×3）＋ 三条顺手（M8/M9/M16）全部落地
- [ ] **全章 grep 自查**：`两种子`／`两随机种子`／`独立采样`／`样本不重合` 在十节中已无错误实例（在线特权消融那处 n=2 应仍在）
- [ ] `latexmk -pdf -xelatex -interaction=nonstopmode main.tex` 通过：58 页、末次 pass 0 undefined、无 Overfull>10pt（基线：2026-07-21 实测为 2 处 Overfull，分别 3.1pt / 4.3pt）；成功后 `latexmk -c` 清中间文件
- [ ] `chapter_full_review_findings.md` 对应条目后追注闭环（沿用 §5.8/§5.9/§5.10 findings 的追注体例）
- [ ] `status.md` 更新：新增「全章整体复审轮」行 + 整改批次进度
- [ ] commit：`docs:` 前缀 + `Co-Authored-By` trailer，只暂存本轮相关文件，不 push

## 后续批次（本轮不做，供你判断边界用）

| 批次 | 内容 | 流程 |
|---|---|---|
| 第 2 批 | **H1**（§5.8 补第三种读法 + 登记 $\beta_1$ 轴未检验；§5.9.5「相互印证」改为并置张力）＋ **H3**（奖励错配 vs 越界失败形态的正面回应；§5.10.4 锚点扩展句精确到 crosscomp 一格）＋ **H5**（§5.10.3 正面承认中段质量区间的约束）＋ M11、M6 | **重流程**：方案→批准→落地→独立复审 |
| 第 3 批 | **H2**（§5.7.2 等价判据：种子级 95% CI 半宽 13.3pp 远超所声明 ±6pp 容忍界）＋ M13 | 重流程 |
| 第 4 批 | **H4**（时序利用的文献坐标：§5.2 补一支 + §5.4.1 POMDP 补 cite + §5.5.3 补 1–2 条 + refs.bib 扩条目）＋ **H6**（补 1000 回合加噪反证，净加强论证）＋ **H7**（§5.5.4 单调性表述改述）＋ M1、M3、M12 | 混合 |
| 第 5 批 | 余下 MED/LOW ＋ CH2（贡献类型声明前移 §5.1） | 轻流程 |

## 章级现状（详见 `status.md`）

- §5.1–§5.10 十节全部成文；编译 58 页零严重警告；页面分布：§5.1 p1／§5.2 p2／§5.3 p4／§5.4 p11／§5.5 p18／§5.6 p26／§5.7 p30／§5.8 p40／§5.9 p44／§5.10 p50。
- 全章整体复审已确认：数字忠实度**零漂移**（三路独立审计）、统计可复现（Welch $t=0.104$／$df=7.91$ 精确复现）、诚实度体系完整——本批整改**不触碰这些已合格的层面**。
