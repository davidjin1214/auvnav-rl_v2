# 第 5 章 — 轮次入口（**当前轮：数据完整性整改批的独立复核**）

> 体例（2026-07-07 起）：本文件**每轮重写**，只含当轮任务与相关约束。章状态账本 = [`status.md`](status.md)（唯一真相源）；逐轮历史 = git log 与各节头注；prompt 方法论 = [`prompt_playbook.md`](prompt_playbook.md)。
> 上一轮（2026-08-16 裁决 + 整改落地）已闭环。**这是五批整改后第一次动正文**，故按 `prompt_playbook.md` §2 的会话序列，下一步是独立对抗复审。

---

## 走到哪儿了

第 5 章十节成文、2026-07-28 判定送审就绪。章外三条数据完整性问题（①③⑤）已于 2026-08-16 裁决走 **①-c**（正文披露 + 敏感性读数）并合成**一批**整改落地。

- 事实账本：[`../../docs/data_integrity_open_items.md`](../../docs/data_integrity_open_items.md)
- 波及面评估的独立复核 + 两次探针追注（§8 §9）：[`data_integrity_impact_assessment_review.md`](data_integrity_impact_assessment_review.md)
- 本批逐处清单与验收：[`status.md`](status.md) 的「章外三条数据完整性问题」块

### 本批改了什么（4 个文件、9 处）

| 文件 | 处 |
|---|---|
| `setup.tex`（rev.7） | §5.3.5 种子区间关系与两千回合实例重合；§5.3.6 选点集嵌套 + 定义「留出回合」 |
| `rebrac.tex`（rev.5） | §5.7.1 补充终检段；§5.7.2 留出读数 + 「过半」→「约半」；§5.7.m 两段集中登记；筛查表 caption 补 val≡test |
| `td3bc.tex`（rev.7） | §5.6.2 含噪段两端点口径限定；§5.6.m 种子区间与复核读数 |
| `discussion.tex`（rev.3） | §5.10.4 章级限制补一句 |

### 已验收的硬护栏

- 编译 $65$ 页、$0$ undefined ref/citation、$2$ 处 Overfull（$3.1$/$4.3$ pt，与整改前逐条相同）；
- **既有数字零漂移**（四文件正文数字多重集逐一比对，无一被删改）；
- **唯一论证强度改动**：§5.7.2「差距闭合过半」→「约半」（点估计 $53.0\%$ 未变，改的是留出集上不再成立的限定词 $48.9\%$）。

---

## 本轮任务：独立对抗复审（新会话冷启动）

按 `prompt_playbook.md` §2 第 2 步与 §4.2 的复审骨架，换参即可；模板取 [`section_5_6_review_prompt.md`](section_5_6_review_prompt.md)（硬停语义 + 诚实分级最完整版）。审查对象是本批改动，**不是整章重审**。

指名要查的维度（这是本批的特有风险面，复审不要只重复通用清单）：

1. **数字忠实**：新增读数全部可由两个复算工具重出——[`tools/ch5_clean_probe_readout.py`](tools/ch5_clean_probe_readout.py)（① 补充终检）与 [`tools/ch5_holdout_split_audit.py`](tools/ch5_holdout_split_audit.py)（③ 留出复核）。逐个核，**禁止以头注 rev 块或本文件为准**。
2. **口径不越权**：补充终检只覆盖 ReBRAC-Q 两格；正文是否有任何一处把它的结论外推到 TD3+BC／纯 BC，或拿它跨评估集比较绝对值。
3. **限定的可传导性**：§5.7.m 写明的两条边界——留出复核剔除不掉「选点针对这一族调定」、绝对水平下移中约 $2$ pp 归抽样——是否在 §5.7.1、§5.6.2、§5.10.4 的引用处被弱化或丢失（收束层不得强于源头）。
4. **「约半」的一致性**：全章是否还有别处以「过半」或等价强度引用该闭合比例。
5. **术语**：本批新引入「留出回合」与「补充终检」两个词，是否先定义后使用、全章用法一致（定义分别在 §5.3.6 与 §5.7.m）。
6. **红线**：负面读数（$48.9\%$、$35.6\%$、绝对水平下移）是否作 finding 同台呈现，而非降级为附带说明（spec §0.5.6）。

复审后若判定通过，本章恢复送审就绪；若出 CRIT/HIGH，按批修复并复编译。

---

## 硬约束

1. **数字纪律**：不凭记忆、不以成稿自述或其头注为准，一律回查 [`../../docs/rebrac_experiment_report.md`](../../docs/rebrac_experiment_report.md)、`results/offline/**/seed_*.json` 的逐回合记录，或上面两个复算工具。
2. **反 churn**：某段已合格就明说「保留不动」，不为改而改；诚实分级，不为凑评级制造不存在的 CRITICAL。
3. **工程交付**：locked 文件用 Edit 不用 Write；`latexmk -pdf -xelatex` 须 $0$ undefined ref / $0$ undefined citation，成功后 `latexmk -c`。
4. 两条易复发错误：**§5.3.6 正文从未写过「独立测试集」**（该误称出自 `td3bc.tex` rev.6 头注，2026-08-16 已订正，别写回去）；**枚举支撑的全称断言不可信**——`glob` 在网络挂载上返回的"全集"不是全集，须以 `os.listdir` 逐名 stat 复核。

---

## 相关提交

`09d0a44`（① 核实）· `e31cc58`（波及面评估）· `36fc060`（独立复核 + hold60 工具）· `22dac93`（事实账本订正 + 探针 notebook）· `53c2b1e`（首轮探针）· `c72ec44`（notebook 修复）· `6618161`（①-c 补评读数 + 复算工具）
