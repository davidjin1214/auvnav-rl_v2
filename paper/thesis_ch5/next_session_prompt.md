# 第 5 章 — 轮次入口（**当前轮：数据完整性处置裁决 → 一次性整改批**）

> 体例（2026-07-07 起）：本文件**每轮重写**，只含当轮任务与相关约束。章状态账本 = [`status.md`](status.md)（唯一真相源）；逐轮历史 = git log 与各节头注；prompt 方法论 = [`prompt_playbook.md`](prompt_playbook.md)。
> 上一轮（2026-08-16 Drive 侧探针 + ①-c 补评）已闭环，**所有裁决所需的实测读数已到齐**。

---

## 走到哪儿了

第 5 章十节成文、五批整改闭环，2026-07-28 判定送审就绪。章外的数据完整性问题现为**三条**（①③⑤），全部核实成立：

- **①** `crosscomp-2000` 的训练 episode 与终检评估是同一批任务实例（100/100 逐条相同）。
- **③** 选检查点的验证集是终报测试集的**前缀子集**；40+40 的单元两份 manifest 完全相同。
- **⑤** 含噪 2000 集同样 `seed=0`、100/100 逐条相同（与 ① 同因同病）。

事实账本：[`../../docs/data_integrity_open_items.md`](../../docs/data_integrity_open_items.md)。
波及面评估：[`data_integrity_impact_assessment.md`](data_integrity_impact_assessment.md)。
**独立复核 + 两次探针追注（先读这份，§8 §9 是新的）**：[`data_integrity_impact_assessment_review.md`](data_integrity_impact_assessment_review.md)。

### 裁决所需的实测读数（全部可复算，别凭记忆改写）

复算命令：`python paper/thesis_ch5/tools/ch5_clean_probe_readout.py`、`python paper/thesis_ch5/tools/ch5_holdout_split_audit.py`

1. **① 的污染幅度 = 差中差 $+0.80$ pp**（t=0.512，CI $[-3.5,+5.1]$）。干净 manifest（种子 3000..3099，与训练区间及现测试集均不相交）上重跑原封不动的 10 个选中检查点所得。**低于复核预设的 $2$ pp 线，远低于 $5$–$7$ pp 线 → 章内真实断言「未再检出回落」在干净集上依然成立**（翻转点估计 $+1.6 \to +0.8$ pp，仍为正）。
2. **③ 的已刊内效应已量化**（`hold60`）：留出 60 条上组间差**全部保号且变大**——翻转 $+1.6\to+3.0$ pp、基线差距 $23.0\to28.0$ / $32.2\to40.0$ pp、§5.6 回落 $-7.6\to-9.0$ pp。**唯一例外**：§5.7.2「差距闭合过半」$53.0\%\to\mathbf{48.9\%}$，不再过半。
3. **两格绝对水平在干净集上都掉 $4$–$5$ pp 且高度显著**（5/5 种子同向，CI 不含 0）。其中仅约 $2$ pp 是抽样难度（采集器基线 $0.950\to0.930$，解析式策略、无训练无选点）；**余下约 $2$–$3$ pp 是选点族泛化**——检查点是在种子 1250 族的 `val_40` 上选的。**`hold60` 结构上看不见这一层**（留出 60 条仍属同族）。
4. **① 与 ③ 在 2000 格上叠加**：该格选点用的 40 条 val 本身就在训练区间内。
5. **未覆盖**：只补评了 ReBRAC-Q 两格。TD3+BC / 纯 BC 未重评 → §5.7.1 的基线差距**不能**在干净集上重述（两端会一起移动，净方向未知）。

---

## 本轮任务

### 第一步：裁决（用户拍板，agent 不决定）

读数已到齐，四条路径的取舍现在是有证据的选择题：

- **①-a 重采重跑**：读数不支持——① 只值 $0.8$ pp，重采 2000 回合数据集加重跑该单元换不回等量的结论强度。
- **①-b 保留数字 + 降级翻转论述**：可行，但"降级"现在应当是**拿证据降级**，不是无条件撤回。
- **①-c 披露 + 敏感性读数**（本轮已把读数做出来）：把干净集补评作为敏感性分析写进正文/附录。
- **补齐 TD3+BC / 纯 BC 的干净集补评**：若裁决要在正文重述基线差距，这一步是前置条件；否则可不做。

### 第二步：一次性整改批（唯一动 `.tex` 的一步）

**①③⑤ 必须同批**——分批等于付多次重流程复审，而本章「送审就绪」正是靠五批整改逐字节证出来的。批内容随裁决定，至少含：

- ③ 的五处口径句（选点集与测试集的包含关系）+ 复核补的两处漏项（筛查单元无留出集、§5.6.2 而非 §5.6.1）；
- ① 的披露与敏感性读数（若走 ①-c）；
- §5.7.2 那条不利读数（闭合比 $48.9\%$，「过半」不再成立）；
- §5.6.2 noisy-support 段（⑤ 已成立，该段两个端点都在污染数据上）；
- 引用 `hold60` 处须带上限定：它量化的是 ③ 的**已刊内**效应，不含选点族泛化。

---

## 硬约束

1. **在裁决之前不动任何 `.tex`。** 动正文即触发 `prompt_playbook.md` 的重流程复审义务，五批整改逐字节证明过的「零论证／零数字漂移」性质随之失效。
2. **不凭记忆写数字。** 每条都应能核到 `.tex` 正文、[`../../docs/rebrac_experiment_report.md`](../../docs/rebrac_experiment_report.md)（数字唯一权威源）、数据集 `metadata.json`、`results/offline/**/seed_*.json` 的逐回合记录，或上面两个复算工具。
3. **处置不由 agent 决定。** 给判断和依据，裁决权在用户。
4. 已知的两条易复发错误：**§5.3.6 正文从未写过「独立测试集」**（该误称出自 `td3bc.tex` rev.6 头注，2026-08-16 已订正）；**污染面枚举不可信**——`glob` 在网络挂载上返回的"全集"不是全集，凡以枚举支撑全称断言者须改用 `os.listdir` 逐名 stat 复核。

---

## 相关提交

`09d0a44`（① 核实）· `e31cc58`（波及面评估）· `36fc060`（独立复核 + hold60 工具）· `22dac93`（事实账本订正 + 探针 notebook）· `53c2b1e`（首轮探针读数）· `c72ec44`（notebook 协议头修复）
