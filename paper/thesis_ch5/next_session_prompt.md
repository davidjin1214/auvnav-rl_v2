# 第 5 章 — 轮次入口（**当前轮：数据完整性波及面评估的独立复核**）

> 体例（2026-07-07 起）：本文件**每轮重写**，只含当轮任务与相关约束。章状态账本 = [`status.md`](status.md)（唯一真相源）；逐轮历史 = git log 与各节头注；prompt 方法论 = [`prompt_playbook.md`](prompt_playbook.md)。
> 上一轮（2026-07-28 收尾统稿轮）已闭环、本章送审就绪，其结论与「若要再开一轮先读这三条」见 git 历史版本与 `status.md`。

---

## 背景

第 5 章十节成文、五批整改闭环，2026-07-28 判定送审就绪。之后（2026-08-09）在章外查出两条数据完整性问题，**均已核实成立**：

1. **`crosscomp-2000` 数据集的训练 episode 与终检评估用的是同一批任务实例**——数据集 `seed=0`、2000 回合，训练种子 0..1999 完整包含评估 manifest 的 1250..1349；reset RNG 重放确认 100/100 逐条相同。
2. **选检查点的验证集（40 回合）是终报测试集（100 回合）的前缀子集**——manifest 生成器无 seed 偏移，val/test 用同一 benchmark key；40+40 的单元里两份 manifest 完全相同。§5.3.6 现写的「独立测试集」不成立。

事实与证据链：[`../../docs/data_integrity_open_items.md`](../../docs/data_integrity_open_items.md)。复核工具：`python -m scripts.audit_seed_overlap`（`--verify DATASET MANIFEST` 做实例级比对）。相关提交：`09d0a44`。

在此之上起草了一份**波及面评估**，用于支撑处置裁决：

**[`data_integrity_impact_assessment.md`](data_integrity_impact_assessment.md)（初稿，未经独立复核）**

---

## 本轮任务

**独立复核这份评估，判断它是否可以作为裁决依据。**

它给出的核心图景是：中心命题与四条主要发现都不在污染面上，真正押在受污染数据格上的只有 §5.7.1「数据规模退化的翻转」这一条论述；③ 的影响限于 §5.6/§5.7 且可由既有披露体例吸收。**这个图景可能是对的，也可能漏了东西或过于乐观——请自行判断。**

需要你回答的：

1. 它列的「必然要改的」是否完整——有没有漏掉的表、图、数字、论述或跨节承接；
2. 它列的「守得住的」是否真守得住——尤其超参数 $(\beta_1,\beta_2)$ 的选择证据链含污染块这一点，它的「不塌」结论是否成立；
3. 它自己标为**推断**的三条（§6 打 ★）——「污染只会抬高 2000 格」「共模偏差在组间差值中大部分抵消」「翻转可能被推翻」——各自的可靠度如何，是否有更强或更弱的说法；
4. §5 的四个处置选项与起草者倾向（①-a + ③-b）是否合理，有没有被漏掉的第三条路。

结论请分级给出（成立／需修正／不成立），每条附可核的依据。发现评估本身有事实错误时直接指出并给出正确值。

---

## 硬约束（只有三条）

1. **不改任何 `.tex`。** 本轮是复核，不是整改。本章送审就绪的性质由五批整改逐字节证明过，动正文即使这些证明失效、须按 `prompt_playbook.md` 重跑重流程——那是裁决之后另开一轮的事。
2. **不要相信评估文档的自述，也不要凭记忆写数字。** 它的每一条都应能在 `.tex` 正文、[`../../docs/rebrac_experiment_report.md`](../../docs/rebrac_experiment_report.md)（数字唯一权威源）、数据集 `metadata.json` 或代码里核到。这份评估是被审对象，不是依据。
3. **处置不由你决定。** 给判断和依据，裁决权在用户。

方法、工具、检索路径、产出形式由你定。产出落到 `paper/thesis_ch5/` 下，文件名自拟。
