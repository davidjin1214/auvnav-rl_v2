# 任务：按 §5.5 的标准，系统性复审博士论文第 5 章 §5.6（离线基线：行为约束方法的两个瓶颈）

> 用法：在新对话中以 `/academic-research-skills:academic-paper` 调起本 prompt（与 §5.5 复审同一流程）。

## 你的身份
你是 AUV（水下自主航行器）运动规划 × 强化学习交叉领域的资深专家，同时是顶会/顶刊
（NeurIPS / ICLR / CoRL 级）RL 论文的作者与严格审稿人，以及中文博士论文的外审人。
请以专家眼光自由判断；若发现内容组织或写作本身有问题，直接指出，不要迁就现状。

## 本轮性质（重要：先诊断、给方案、硬停待确认、后改写）
不要一上来就动笔。流程必须是：
  1) 通读当前状态（见"必读清单"）；
  2) 产出分级诊断复审（CRITICAL / HIGH / MEDIUM / LOW）+ **成文的完整修改方案**——每条问题给出
     "改什么 / 怎么改（必要时含拟改写的关键句或表/图示意）/ 为何改"，并含三项明确建议：是否新增
     图、§5.6.2 子节去留、§5.k.m 实施细节归属；
  3) ⚠ **硬停（仅限文件改动）**：呈现诊断与修改方案后停下来，进入"确认方案"环节——这一环节中
     **可以用 AskUserQuestion 就关键结构决策（是否新增图 / §5.6.2 去留 / §5.k.m 归属等）与我对齐**，
     我也会通读、评论、调整方案；但在我明确确认方案（"开始修改 / 按此执行 / 同意"之类）**之前，
     绝不编辑任何文件、绝不调用 Edit / Write / 绘图 / 编译工具**。即硬停只卡"文件改动"，不卡对齐性提问；
  4) 仅在我确认方案后，才进入修改阶段：改写、绘图、编译核验、提交。
authoring（写）与 review（评）分两个独立 pass，不在同一上下文里自我批准。
**诚实分级**：§5.6 已经过一轮四视角对抗复审（见下），数字与红线大体扎实；不要为凑评级制造
不存在的 CRITICAL。把精力放在"是否真达到 §5.5 标准"上。

## §5.5 已确立的标准（本轮以此为尺，逐条对照 §5.6）
§5.5（在线节）刚完成多轮复审并定稿，确立了以下可复用标准，§5.6 须达到同一水平：
- **发现链组织**：结果节以"实验当时的提问顺序"层层推进，每个子节回答一个明确问题、以
  一图/一表支撑、问题→证据→结论三者齐备；避免机械套用"设置/结果/消融/复现"模板。
- **图文数字一致**：表、图、正文、图注中同一量完全一致。
- **每个发现一图/一表**：§5.5 六图各支撑一个子发现（学习曲线/柱状/反向轴/单调剂量/经验上界）。
  ⚠ §5.6 目前**只有两张表、没有任何图**——这是与 §5.5 标准最显眼的差距，须重点判断
  （见重点 2 与插图规范）。
- **语言正式**：不出现代码/变量/参数/奖励代号痕迹；无自造词；无防御性辩解（但保留必要的
  科学严谨，且用陈述语气）；方法学只交代一次；机制保持假说语气。
- **章级实施细节归属**（已确立方案，见重点 4）：§5.3=一次性实验协议唯一之家、§5.4=算法实现
  之家、各结果节 §5.k.m=仅本节增量 + 指针。

## 必读清单（先读，再诊断；禁止凭记忆写数）
- 章工程：`paper/thesis_ch5/main.tex` 与 `sections/` 下 intro / related_work / setup /
  methodology / online / td3bc。【§5.6 = `sections/td3bc.tex`】
  - 尤其先读 **§5.5 = `sections/online.tex`** 作为标准范例（看它如何组织发现链、如何处理
    实施细节归属、如何守红线），再读 §5.3 `setup.tex`（§5.3.6 评估协议已定义"终检评估/
    周期评估"两口径，是全章评估口径之家）与 §5.4 `methodology.tex`（TD3+BC 损失
    `eq:ch5_td3bc_actor`、α_BC、检查点选择等算法基础之家）。
- 写作规范 spec：`paper/thesis_chapter_outline.md`
  （§0.4 机制归因红线 / §0.5 体裁规范含 §0.5.10 术语对照 / §1 章骨架与节号 / §2 §5.6 素材映射
  / §0.2–§0.3 中心命题与四支撑角色）。
- 数据真相（唯一权威，逐项回查、禁止照抄 headline）：
  - `docs/td3bc_phase0c_experiment_report.md`（§5.3 Stage C 主表 / §5.4 同预算纯 BC 对照 /
    §3.2 数据集统计 / §6 noisy-support 筛查 / §7.2 worldcomp formal）；
  - `docs/rebrac_line_overview.md` §1（headline 镜像，仅作交叉核对）；
  - 离线线背景：`docs/offline_rl_line_summary.md`。
- 记忆：`~/.claude` 项目 memory 下 `project_paper_writing_backlog_ab.md`（章进度，已含 §5.5
  复审 + 章级 §5.k.m 归属方案）、`feedback_figure_standards.md`（插图 6 条硬规范）。
- 图样式：`paper/thesis_ch5/figures/scripts/_ch5_style.py` + `_ch5_data.py`（若需为 §5.6 新作图，
  复用此共享样式与真实数据加载器；§5.5 的 `fig_ch5_online_*.py` 是风格与"结果图画真实数字"的范例）。

## 当前状态（截至最近提交）
§5.6 已落地并经 **rev.2 四视角独立对抗复审**（数字忠实 / 语体术语 / 论证统计 / 跨节红线），
rev.3 完成与 §5.3 的章级种子约定一行对齐。现有子节骨架（以实际文件为准）：
  §5.6.1 从协议伪象到真实的非单调现象 → §5.6.2 本节的实验增量 → §5.6.3 瓶颈之一：数据支持集
  的结构 → §5.6.4 瓶颈之二：可部署价值网络的信息瓶颈 → §5.6.5 两个瓶颈对离线主线方法的动机
  → §5.6.m 实施细节与可复现性。
证据：两张表（数据规模轴 `tab:ch5_td3bc_size`、worldcomp 协议对照 `tab:ch5_td3bc_worldcomp`），
**无图**。rev.2 已处理：48.5%/6pp 的 n=5 不显著性（瓶颈二论证重心移到"可部署最优退回纯 BC"
这一稳健信号、特权增益降为"机制方向提示"）、弯引号、枚举溶解、teacher 禁令、LayerNorm 排除等。
本轮不是推翻已确认数据，而是按 §5.5 标准复审"内容组织"与"语言表达"，并补足图证据缺口。

## 五个重点

### 1. 篇幅取向：讲透 > 压缩
长篇中文博士论文，不以压缩篇幅为目标，而以把问题讲清楚、讲透为标准。可充分展开推理、机制与
证据链；但"讲透"≠注水/重复，每段须有信息增量。

### 2. 内容组织：发现链是否真服务，且证据呈现是否达 §5.5 标准
以专家身份认真思考高水平 RL 论文结果节如何组织，再结合本节实际：
  - **还原发现链**：直觉（数据越多越好）→ 早期反常（单调下降）被识破为协议伪象 → 校正后留下
    真实的非单调（峰值在中等规模、最大规模回落）→ 为何（瓶颈一：支持集结构；瓶颈二：可部署
    价值网络信息）→ 对离线主线方法意味什么（双侧行为约束动机）。审视现有子节是真服务这条链，
    还是套模板。
  - **重新审视独立的 §5.6.2"本节的实验增量"子节**：§5.5 的范例是把最小化的 setup 就近内联到
    各结果子节、每子节回答一个问题；§5.6 却把 setup 抽成独立子节。请判断：§5.6.2 是服务发现链
    （两个瓶颈共享 α 扫描/数据集/纯 BC 对照等实验机件，集中交代更省），还是打断"现象→瓶颈"的
    推进、成了机械"设置"模板？给出章级一致的判断（与 §5.5 的内联式是否需要统一）。
  - **图证据缺口（关键）**：§5.6 目前只有两张表、无图。按 §5.5"每发现一图/一表"的标准，"数据
    越多反更差"的非单调倒 U（TD3+BC 与纯 BC 双曲线 × 三规模）是极具说服力的可视化，spec §5
    图清单也预留了"(5.6) '2000<1000' 退化 + 纯 BC 对照（可选）"。请判断是否新作一张结果图
    （若作，须画真实数字、含误差棒、守 6 条插图规范）；以及瓶颈二的 worldcomp 三行是否也宜图示。
  - 本节选定的组织原则应与 §5.5 协调，可被后续 §5.7… 复用。

### 3. 语言：正式论文，非实验报告/备忘录
  - 不得引入代码/实验文档痕迹：数据集目录名、程序变量名、命令行参数名、奖励配置代号
    （efficiency_v2 / arrival_v2 / crosscomp / worldcomp / td3bc 之类）一律不出现；只用论文级
    中文术语（横流补偿参照 / 世界坐标系流速补偿参照 / 历史效率导向奖励 / 行为克隆 等）。已定义
    的数学符号（行为克隆权重 $\alpha_{\mathrm{BC}}$、$s_0$、$k$ 等）可用，但须承 §5.3/§5.4 的首次定义。
  - 不得有自造词/不规范表达；不得有防御性辩解腔。但要区分"防御性辩解"与"必要的科学严谨"——
    48.5%/6pp 的 n=5 不显著、"机制方向提示"、noisy-support 两种子筛查 caveat 属严谨，**保留**，
    但须陈述语气、且不过度对冲到削弱发现本身。
  - 不在正文反复强调种子数量/个别种子；方法学只交代一次（细节入 §5.6.m）。

### 4. "实施细节与可复现性"小节的章级归属（已确立方案，本节须合规）
§5.5 复审已确立并落地章级方案，§5.6 须照此自查：
  - **§5.3（设置）= 一次性实验协议唯一之家**：环境 / 传感 / 奖励双轨 / 评估集 / 回合数 / 种子
    策略 / **终检评估·周期评估命名**。
  - **§5.4（方法）= 算法实现之家**：网络 / 损失 / 超参 / 特权观测协议 / **检查点选择规则**。
  - **各结果节 §5.k.m = 仅本节增量 + 一行指针**（用哪些数据集、本实验种子数、本节特有计算）。
  逐条核查 §5.6.m：① 检查点字典序选择规则是否应上提 §5.4.m（避免与 §5.7.m 重复），还是确为
  TD3+BC 本节特有；② §5.6 正文/表是否应采用 §5.3.6 已定义的"终检评估"口径名（现仅写"正式多
  随机种子协议下评估 100 回合"，与 §5.5 命名不齐）；③ 是否仍有与 §5.3/§5.4 重复的章级交代未
  收为指针。给出与 §5.5 一致的最终归属。

### 5. 其他（专家补充要求）
  - **图文数字一致**：逐一核对表/正文/图注/（若新增）图中同一量完全一致，尤其
    crosscomp 0.592/**0.672**/0.596、纯 BC 0.524/**0.588**/0.534、采集成功率 0.864/0.870/0.886、
    worldcomp 可部署 0.858 / 特权 0.922 / 采集器 0.990、6pp≈48.5%（=0.064/0.132）、noisy 0.60→0.74。
  - **机制保持假说语气**：支持集"过度致密"机制、特权信息"方向性提示"均按提示/很可能措辞，不过断言。
  - **每个实验子节闭环**：问题 → 证据（图/表）→ 结论，三者齐备。
  - **结果节图须画真实结果数字**（区别于设置/方法节"零结果数字"图）；不得编造未取回的数字
    （ground-truth 不含"最近邻动作距离度量"，已按"不凭记忆/不编造"删去该度量——不要重新引入）。
  - **章节自足、跨节纪律**：§5.5（在线节）已落地，§5.6 可 `\ref{sec:ch5_online}`（开篇已用），承
    §5.3/§5.4/§5.5；但**不 `\ref` 尚未落地的 §5.7 及之后**（用散文前指即可）。
  - 红线（务必守住）：
      · **online↔offline 特权 critic 方向相反，须严防自相矛盾**：§5.5（在线 SAC 反向轴）给价值
        网络注特权流**不闭合 gap / 反降**；§5.6（离线 TD3+BC 基线）特权 critic **弱助**（+6pp,
        n=5 不显著）。二者方法/情形不同、不矛盾，但外审会并读追问。§5.6 须把"特权弱助"严格
        scope 为**仅刻画 TD3+BC 基线限制 + §5.7 的问题设定**（"能否不借特权、用更强价值侧约束
        让可部署价值网络自给可靠改进信号"），**不**与 §5.5 反向轴并列下"特权普遍有/无用"的结论，
        **不**跨线断言。判断是否需要一句明确对照 §5.5（厘清方向差异、避免误读）还是反而徒增混淆。
      · **"2000<1000"反规模（anti-scaling）严格限定**在 $s_0$ + 卡门涡街 + REMUS-100 边界内，
        不包装为 algorithm-paper 的普适 claim（spec R7/R8）。
      · **不把性能差距归给策略侧 anchor**（actor β1 等是 §5.7 的事）；**不提 LayerNorm**
        （§5.7 表征稳定性轴，TD3+BC 不含）。
      · **teacher/蒸馏禁令**：0.990 写作"采集器自身成功率 / 高质量参照水平"，不写 teacher。
      · **瓶颈二论证重心**须落在稳健信号（可部署最优退回纯 BC，价值项无净增益），特权增益只作
        方向性提示，不以 48.5% 精确值定论（48.5% 仅在 §5.6.m 出现一次）。
      · **负面/退化结果作 finding，不降级为 limitation**。

## 数据口径（全节统一，复审须核对；以 ground-truth 文档为准，不确定就回查，绝不臆造）
- 数据规模轴（横流补偿参照、历史效率导向奖励、$s_0$、$k=4$，正式 5 种子、每种子终检 100 回合）：
  TD3+BC 500/1000/2000 = 0.592±0.119 / **0.672±0.045** / 0.596±0.036；同预算纯 BC =
  0.524±0.053 / **0.588±0.070** / 0.534±0.079（峰值均在 1000、2000 回落）；数据集采集成功率
  0.864 / 0.870 / 0.886（更多数据反更差，排除"数据变坏"）；经后验选择最优 α_BC ≈ 0.5 / 0.25 / 0.15。
- noisy-support 筛查（2000 回合、2 种子、低成本）：纯 BC 0.60→0.7375（约 0.74）；噪声 σ≈0.05、
  截断≈0.15；结论限机制指示、不与正式主表同权。
- 可部署价值网络信息（世界坐标系流速补偿参照、5 种子）：采集器自身 0.990；可部署协议 α→0 =
  0.858±0.080；特权协议 α≈0.1 = 0.922±0.086；点估计 +6.4pp = 0.064/0.132 ≈ 48.5%（n=5、σ≈0.08，
  Welch 下不显著，只作方向提示）。
- 任何数字以上述及 ground-truth 文档为准。

## 插图规范（6 条硬性，详见 memory `feedback_figure_standards.md`）
1 宽≤138mm；2 A4 下字号清晰（≥7pt）；3 曲线/配色可区分不接近；4 元素不重叠（尤其文字与
其他元素）；5 图内少字、解释移图注/正文；6 Nature 顶刊风格。共享样式 `_ch5_style.py`、真实数据
加载 `_ch5_data.py`。**若决定为 §5.6 新作图：先出 1 张样图确认风格再定稿；结果图画真实数字、
含误差棒；不编造未取回的数字。**

## 工作流程
1 通读必读清单（含 §5.5 范例）→ 2 分级诊断复审 + **成文完整修改方案**（含"是否新增图"
"§5.6.2 去留""§5.k.m 归属"三项明确建议）→ 3 ⚠ **硬停（仅限文件改动）**：呈现方案后停下，进入确认环节——此
环节**可用 AskUserQuestion 就关键决策与我对齐**，但在我明确确认方案前**不编辑任何文件、不调用
Edit/Write/绘图/编译工具** → 4（我确认方案后）改写（locked 文件用 Edit，不用 Write；
§5.6 = `sections/td3bc.tex`）
→ 5 如决定则按图规范作图/重绘 → 6 编译核验（0 undefined ref/citation、bibtex 0 warning、
0 overfull>10pt；XeLaTeX：`latexmk -pdf -xelatex -interaction=nonstopmode -halt-on-error main.tex`；
注意：latexmk 中间 pass 会报 undefined，须以"清后单跑一遍 xelatex"的最终 pass 为准判定是否真
undefined）→ 7 `latexmk -c` 清中间文件 → 8 提交（`docs:` 前缀，不加 Co-Authored-By；只暂存
本节相关文件，勿带入无关文件）。
编译坑：改 `.bib` 后须手动 `rm main.bbl main.aux main.fdb_latexmk` 再从零跑（`latexmk -C` 不可靠）；
本章已知 Overfull 在 `methodology.tex` 公式（非 §5.6）。

## 范围
本轮主改 §5.6。§5.3/§5.4/§5.5 或后续结果节的任何结构调整（尤其"§5.6.2 去留""检查点规则
上提 §5.4""终检/周期命名采用"等跨节项）先提案 + 对齐，由我决定本轮一并执行还是分步推进——
不要未对齐就大改其他节。
